import { readFile, writeFile } from 'node:fs/promises';
import { spawn } from 'node:child_process';

const SOURCE = new URL('./stage-a-resolver-derived-contract-v2-pilot.mjs', import.meta.url);
const OUT = new URL('./.stage-a-nontw-discrete-exact-generated.mjs', import.meta.url);
let source = await readFile(SOURCE, 'utf8');

function replaceOnce(label, before, after) {
  const first = source.indexOf(before);
  if (first < 0) throw new Error(`Non-TW exact patch anchor missing: ${label}`);
  if (source.indexOf(before, first + before.length) >= 0) throw new Error(`Non-TW exact patch anchor not unique: ${label}`);
  source = source.slice(0, first) + after + source.slice(first + before.length);
}

replaceOnce(
  'runtime registry import',
  `import { getRuntimeMasterEntry } from '../src/catalog/runtime-master/runtime-master-registry.mjs';`,
  `import { getRuntimeMasterEntry, loadRegisteredRuntime } from '../src/catalog/runtime-master/runtime-master-registry.mjs';`,
);

replaceOnce(
  'window-local terminal multi proof state',
  `  let calls=1,states=0,hits=0,maxDepth=0,blocker=null;\n  const memo=new Map(),inProgress=new Set();\n  const root=await resolveRuntimeAppProduct(row.id,{window_type:row.windowType});`,
  `  let calls=1,states=0,hits=0,maxDepth=0,blocker=null;\n  const memo=new Map(),inProgress=new Set();\n  const terminalMultiCache=new Map(),structuralAuditCache=new Map();\n  let terminalMultiCacheHits=0,terminalMultiCacheMisses=0,terminalMultiTransitionChecks=0;\n  let structuralAuditCacheHits=0,structuralAuditCacheMisses=0;\n  const MAX_TERMINAL_BFS_STATES=Number(process.env.STAGE_A_MAX_TERMINAL_BFS_STATES??4096);\n  const runtimeEntry=getRuntimeMasterEntry(row.manufacturer,row.series);\n  let productModuleStructural=null,ewStructuralModel=null;\n  const root=await resolveRuntimeAppProduct(row.id,{window_type:row.windowType});\n\n  const selectorMentions=(selector,key)=>{\n    if(!selector||typeof selector!=='object')return false;\n    if(Array.isArray(selector))return selector.some((one)=>selectorMentions(one,key));\n    for(const [name,value] of Object.entries(selector)){\n      if(name===key)return true;\n      if(['any','anyOf','all','allOf','not'].includes(name)&&selectorMentions(value,key))return true;\n    }\n    return false;\n  };\n  const selectorMentionsAny=(selector,keys)=>[...keys].some((key)=>selectorMentions(selector,key));\n  const collectSelectorRefs=(node,keys,path='root',out=[])=>{\n    if(!node||typeof node!=='object')return out;\n    if(Array.isArray(node)){node.forEach((value,index)=>collectSelectorRefs(value,keys,path+'['+index+']',out));return out;}\n    for(const [name,value] of Object.entries(node)){\n      if((name==='selector'||name==='when')&&selectorMentionsAny(value,keys))out.push(path+'.'+name);\n      collectSelectorRefs(value,keys,path+'.'+name,out);\n    }\n    return out;\n  };\n  const sameStrings=(left,right)=>stableJson([...left].map(String).sort())===stableJson([...right].map(String).sort());\n  const exactSubset=(resolved,key,subset)=>sameStrings(normalizeMulti(Array.isArray(resolved.selection?.[key])?resolved.selection[key]:[]),subset);\n  const terminalContextKey=(result,field)=>{\n    const deps=[...ancestors(depMap,field.key)].sort();\n    const context={};\n    for(const key of deps)if(Object.prototype.hasOwnProperty.call(result.selection??{},key))context[key]=result.selection[key];\n    return hash({field:field.key,context,field_signature:fieldSig(field),custom_pending:customPending(result)});\n  };\n  const terminalStateKey=(result,fieldKey)=>{\n    const current=(result.fields??[]).find((candidate)=>candidate.key===fieldKey)??null;\n    const subset=normalizeMulti(Array.isArray(result.selection?.[fieldKey])?result.selection[fieldKey]:[]).map(String);\n    return stableJson({subset,field_signature:fieldSig(current),validation:result.validation?.status??result.status??null,custom_pending:customPending(result)});\n  };\n  async function getProductModuleStructural(){\n    if(productModuleStructural)return productModuleStructural;\n    const pkg=await loadFormalProductRuntimePackage(runtimeEntry);\n    const preferred=runtimeEntry?.productModuleRole&&pkg.documents?.[runtimeEntry.productModuleRole];\n    const doc=preferred??Object.values(pkg.documents??{}).find((value)=>value?.product_module);\n    productModuleStructural=doc?.product_module??null;\n    return productModuleStructural;\n  }\n  async function getEwStructuralModel(){\n    if(ewStructuralModel)return ewStructuralModel;\n    const runtime=await loadRegisteredRuntime('LIXIL','EW');\n    ewStructuralModel=runtime?.master?.canonicalWorkbook??null;\n    return ewStructuralModel;\n  }\n  async function verifyIndependentPowerSetAudit(result,field,candidates){\n    const fieldKey=field.key,baseSignature=fieldSig(field),upstream={...(result.selection??{})};delete upstream[fieldKey];\n    const beforeChecks=terminalMultiTransitionChecks;\n    if(calls>=MAX_CALLS)throw Object.assign(new Error('resolver call limit before structural empty audit'),{code:'RESOLVER_CALL_LIMIT_REACHED',field:fieldKey,calls});\n    const emptyResolved=await resolveRuntimeAppProduct(row.id,upstream);calls++;terminalMultiTransitionChecks++;\n    const emptyField=(emptyResolved.fields??[]).find((candidate)=>candidate.key===fieldKey)??null;\n    if(!emptyField||!sameStrings(enabled(emptyField).map((choice)=>choice.value),candidates)||!exactSubset(emptyResolved,fieldKey,[]))return null;\n    for(let i=0;i<candidates.length;i++){\n      if(calls>=MAX_CALLS)throw Object.assign(new Error('resolver call limit during structural singleton audit'),{code:'RESOLVER_CALL_LIMIT_REACHED',field:fieldKey,calls});\n      const subset=[candidates[i]],child=await resolveRuntimeAppProduct(row.id,{...upstream,[fieldKey]:subset});calls++;terminalMultiTransitionChecks++;\n      const current=(child.fields??[]).find((candidate)=>candidate.key===fieldKey)??null;\n      if(invalid(child)||!current||!exactSubset(child,fieldKey,subset)||stableJson(fieldSig(current))!==stableJson(baseSignature))return null;\n    }\n    for(let i=0;i<candidates.length;i++)for(let j=i+1;j<candidates.length;j++){\n      if(calls>=MAX_CALLS)throw Object.assign(new Error('resolver call limit during structural pair audit'),{code:'RESOLVER_CALL_LIMIT_REACHED',field:fieldKey,calls});\n      const subset=normalizeMulti([candidates[i],candidates[j]]),child=await resolveRuntimeAppProduct(row.id,{...upstream,[fieldKey]:subset});calls++;terminalMultiTransitionChecks++;\n      const current=(child.fields??[]).find((candidate)=>candidate.key===fieldKey)??null;\n      if(invalid(child)||!current||!exactSubset(child,fieldKey,subset)||stableJson(fieldSig(current))!==stableJson(baseSignature))return null;\n    }\n    return {verified:true,audit_transition_checks:terminalMultiTransitionChecks-beforeChecks};\n  }\n  const exactSelectorPredicate=(selector,key,op)=>{\n    if(!selector||typeof selector!=='object'||Array.isArray(selector))return null;\n    const value=selector[key];\n    if(!value||typeof value!=='object'||Array.isArray(value))return null;\n    const entries=Object.entries(value);\n    if(entries.length!==1||entries[0][0]!==op||entries[0][1]===undefined||entries[0][1]===null||Array.isArray(entries[0][1]))return null;\n    return String(entries[0][1]);\n  };\n  const samosConflictEdge=(a,b)=>[String(a),String(b)].sort().join('::');\n  const independentSetCount=(candidates,edges,required)=>{\n    const nodes=[...new Set(candidates.map(String))],adjacency=new Map(nodes.map((id)=>[id,new Set()]));\n    for(const [a,b] of edges){if(a===b||!adjacency.has(a)||!adjacency.has(b))continue;adjacency.get(a).add(b);adjacency.get(b).add(a);}\n    const isolated=nodes.filter((id)=>adjacency.get(id).size===0),remaining=new Set(nodes.filter((id)=>adjacency.get(id).size>0));\n    let total=1n<<BigInt(isolated.length);\n    while(remaining.size){\n      const seed=remaining.values().next().value,component=[],queue=[seed];remaining.delete(seed);\n      while(queue.length){const id=queue.pop();component.push(id);for(const next of adjacency.get(id))if(remaining.delete(next))queue.push(next);}\n      const componentMemo=new Map();\n      const count=(items)=>{\n        if(!items.length)return 1n;\n        const sorted=[...items].sort(),memoKey=sorted.join('|');if(componentMemo.has(memoKey))return componentMemo.get(memoKey);\n        const first=sorted[0],without=sorted.slice(1),excluded=count(without),blocked=adjacency.get(first),included=count(without.filter((id)=>!blocked.has(id))),value=excluded+included;\n        componentMemo.set(memoKey,value);return value;\n      };\n      total*=count(component);\n    }\n    return total-(required?1n:0n);\n  };\n  async function exactSamosConflictGraph(result,field){\n    if(!['SER-LIX-SAMOS2H','SER-LIX-SAMOSL'].includes(row.id)||field.key!=='options')return null;\n    const module=await getProductModuleStructural();if(!module)return null;\n    const key=field.key,influenceKeys=new Set([key]);\n    for(const valueRow of module.allowedValues??[])if(valueRow.specificationKey===key)for(const metadataKey of Object.keys(valueRow.metadata??{}))influenceKeys.add(metadataKey);\n    const mentionsAny=(selector)=>selectorMentionsAny(selector,influenceKeys),unsupported=[],templates=new Map();let sourceRefCount=0;\n    for(const def of module.specificationDefinitions??[])if(mentionsAny(def.selector)){sourceRefCount++;unsupported.push({kind:'definition',id:def.id??def.key??null});}\n    for(const rule of module.requiredFieldRules??[])if(mentionsAny(rule.selector)){sourceRefCount++;unsupported.push({kind:'required',id:rule.id??rule.specificationKey??null});}\n    const ruleSetRefs=collectSelectorRefs(module.ruleSets??[],influenceKeys,'ruleSets');sourceRefCount+=ruleSetRefs.length;for(const path of ruleSetRefs)unsupported.push({kind:'ruleSet',id:path});\n    for(const valueRow of module.allowedValues??[]){\n      if(!mentionsAny(valueRow.selector))continue;sourceRefCount++;\n      if(valueRow.specificationKey!==key){unsupported.push({kind:'allowed_other_field',id:valueRow.id??valueRow.specificationKey??null});continue;}\n      const other=exactSelectorPredicate(valueRow.selector,key,'$notContains');\n      if(!other||valueRow.value===undefined||valueRow.value===null){unsupported.push({kind:'allowed_unsupported',id:valueRow.id??valueRow.value??null});continue;}\n      templates.set(samosConflictEdge(valueRow.value,other),{kind:'MUTEX_ALLOWED_VALUE_NOT_CONTAINS',a:String(valueRow.value),b:other,source_id:valueRow.id??null});\n    }\n    for(const dep of module.dependencies??[]){\n      if(!mentionsAny(dep.when))continue;sourceRefCount++;\n      const trigger=exactSelectorPredicate(dep.when,key,'$contains'),mode=dep.mode??dep.evaluation,action=dep.action??dep.effect?.type,targetField=dep.targetField??dep.effect?.key,targetValue=dep.targetValue??dep.effect?.value;\n      if(!trigger||mode!=='AUTO'||!['deny_candidate','DENY_CANDIDATE'].includes(action)||targetField!==key||targetValue===undefined||targetValue===null){unsupported.push({kind:'dependency_unsupported',id:dep.id??dep.rule_id??null});continue;}\n      templates.set(samosConflictEdge(trigger,targetValue),{kind:'MUTEX_AUTO_DENY',a:trigger,b:String(targetValue),source_id:dep.id??dep.rule_id??null});\n    }\n    if(sourceRefCount===0)return null;\n    const sourceAudit={adapter:'PRODUCT_MODULE_RUNTIME_V1',model:'SOURCE_DERIVED_PAIRWISE_CONFLICT_GRAPH_V4',influence_keys:[...influenceKeys].sort(),selector_reference_count:sourceRefCount,unsupported_reference_count:unsupported.length,source_templates:[...templates.values()].sort((a,b)=>samosConflictEdge(a.a,a.b).localeCompare(samosConflictEdge(b.a,b.b)))};\n    if(unsupported.length)throw Object.assign(new Error('unsupported Samos options self-reference shape'),{code:'SAMOS_OPTIONS_SELF_REFERENCE_UNSUPPORTED',field:key,detail:unsupported,source_audit:sourceAudit});\n    const candidates=enabled(field).map((choice)=>String(choice.value)),candidateSet=new Set(candidates),possible=sourceAudit.source_templates.filter((edge)=>candidateSet.has(edge.a)&&candidateSet.has(edge.b));\n    const auditKey=hash({model:'SAMOS_SOURCE_AUDITED_CONFLICT_GRAPH_V4',product:row.id,window_type:row.windowType,field_signature:fieldSig(field),templates:possible,influence_keys:sourceAudit.influence_keys});\n    let audit=structuralAuditCache.get(auditKey);\n    if(audit){structuralAuditCacheHits++;}\n    else{\n      structuralAuditCacheMisses++;const beforeChecks=terminalMultiTransitionChecks,upstream={...(result.selection??{})};delete upstream[key];\n      if(calls>=MAX_CALLS)throw Object.assign(new Error('resolver call limit before Samos conflict empty audit'),{code:'RESOLVER_CALL_LIMIT_REACHED',field:key,calls});\n      const emptyResolved=await resolveRuntimeAppProduct(row.id,upstream);calls++;terminalMultiTransitionChecks++;const emptyField=(emptyResolved.fields??[]).find((candidate)=>candidate.key===key)??null;\n      if(!emptyField||!sameStrings(enabled(emptyField).map((choice)=>choice.value),candidates)||!exactSubset(emptyResolved,key,[]))throw Object.assign(new Error('Samos options empty-state audit mismatch'),{code:'SAMOS_OPTIONS_EMPTY_AUDIT_MISMATCH',field:key});\n      for(const id of candidates){\n        if(calls>=MAX_CALLS)throw Object.assign(new Error('resolver call limit during Samos options singleton audit'),{code:'RESOLVER_CALL_LIMIT_REACHED',field:key,calls});\n        const child=await resolveRuntimeAppProduct(row.id,{...upstream,[key]:[id]});calls++;terminalMultiTransitionChecks++;if(invalid(child)||!exactSubset(child,key,[id]))throw Object.assign(new Error('Samos options singleton audit mismatch '+id),{code:'SAMOS_OPTIONS_SINGLETON_AUDIT_MISMATCH',field:key,id});\n      }\n      const templateKeys=new Set(possible.map((edge)=>samosConflictEdge(edge.a,edge.b))),activeEdges=[];\n      for(let i=0;i<candidates.length;i++)for(let j=i+1;j<candidates.length;j++){\n        if(calls>=MAX_CALLS)throw Object.assign(new Error('resolver call limit during Samos options pair audit'),{code:'RESOLVER_CALL_LIMIT_REACHED',field:key,calls});\n        const pair=[candidates[i],candidates[j]],child=await resolveRuntimeAppProduct(row.id,{...upstream,[key]:pair});calls++;terminalMultiTransitionChecks++;const exact=!invalid(child)&&exactSubset(child,key,pair),edgeKey=samosConflictEdge(pair[0],pair[1]);\n        if(templateKeys.has(edgeKey)){if(exact)throw Object.assign(new Error('declared Samos options conflict pair survives '+edgeKey),{code:'SAMOS_OPTIONS_CONFLICT_AUDIT_MISMATCH',field:key,pair});activeEdges.push(pair);}\n        else if(!exact)throw Object.assign(new Error('undeclared Samos options interaction '+edgeKey),{code:'SAMOS_OPTIONS_UNDECLARED_INTERACTION',field:key,pair});\n      }\n      audit={verified:true,active_edges:activeEdges.map(([a,b])=>[String(a),String(b)]),candidate_count:candidates.length,pair_checks:candidates.length*(candidates.length-1)/2,audit_transition_checks:terminalMultiTransitionChecks-beforeChecks,source_audit:sourceAudit};structuralAuditCache.set(auditKey,audit);\n      console.log('SAMOS_CONFLICT_AUDIT window='+row.windowType+' candidates='+candidates.length+' edges='+audit.active_edges.length+' pair_checks='+audit.pair_checks+' calls='+calls);\n    }\n    const terminal=independentSetCount(candidates,audit.active_edges,Boolean(field.required));\n    return {terminal,custom:customPending(result)?terminal:0n,reachable_state_count:terminal,transition_checks:audit.audit_transition_checks,field:key,model:'SAMOS_SOURCE_AUDITED_CONFLICT_GRAPH_V4',candidate_count:candidates.length,edge_count:audit.active_edges.length,audit_cache_key:auditKey,source_audit:audit.source_audit};\n  }\n  async function exactSamosStructural(result,field){\n    if(!['SER-LIX-SAMOS2H','SER-LIX-SAMOSL'].includes(row.id))return null;\n    const module=await getProductModuleStructural();if(!module)return null;\n    const key=field.key,candidates=enabled(field).map((choice)=>choice.value);\n    const influenceKeys=new Set([key]);\n    for(const valueRow of module.allowedValues??[]){\n      if(valueRow.specificationKey!==key)continue;\n      for(const metadataKey of Object.keys(valueRow.metadata??{}))influenceKeys.add(metadataKey);\n    }\n    const refs=[];\n    for(const def of module.specificationDefinitions??[])if(selectorMentionsAny(def.selector,influenceKeys))refs.push('definition:'+(def.id??def.key));\n    for(const value of module.allowedValues??[])if(selectorMentionsAny(value.selector,influenceKeys))refs.push('allowed:'+(value.id??value.specificationKey));\n    for(const rule of module.requiredFieldRules??[])if(selectorMentionsAny(rule.selector,influenceKeys))refs.push('required:'+(rule.id??rule.specificationKey));\n    for(const dep of module.dependencies??[])if(selectorMentionsAny(dep.when,influenceKeys))refs.push('dependency:'+(dep.id??dep.rule_id??'unknown'));\n    refs.push(...collectSelectorRefs(module.ruleSets??[],influenceKeys,'ruleSets'));\n    const sourceAudit={adapter:'PRODUCT_MODULE_RUNTIME_V1',influence_keys:[...influenceKeys].sort(),selector_reference_count:refs.length,selector_references:[...new Set(refs)].sort()};\n    if(sourceAudit.selector_reference_count){\n      if(structuralAuditCacheMisses===0)console.log('SAMOS_STRUCTURAL_SOURCE_AUDIT=REJECT window='+row.windowType+' field='+key+' refs='+sourceAudit.selector_reference_count);\n      return null;\n    }\n    const auditKey=hash({model:'SAMOS_INDEPENDENT_POWER_SET',product:row.id,field_signature:fieldSig(field),source_audit:sourceAudit});\n    let audit=structuralAuditCache.get(auditKey);\n    if(audit){structuralAuditCacheHits++;}\n    else{\n      structuralAuditCacheMisses++;\n      const checked=await verifyIndependentPowerSetAudit(result,field,candidates);\n      audit={verified:Boolean(checked),audit_transition_checks:checked?.audit_transition_checks??0,source_audit:sourceAudit};\n      structuralAuditCache.set(auditKey,audit);\n      console.log('SAMOS_STRUCTURAL_AUDIT window='+row.windowType+' field='+key+' candidates='+candidates.length+' verified='+audit.verified+' checks='+audit.audit_transition_checks);\n    }\n    if(!audit.verified)return null;\n    const terminal=(1n<<BigInt(candidates.length))-(field.required?1n:0n);\n    return {terminal,custom:customPending(result)?terminal:0n,reachable_state_count:terminal,transition_checks:audit.audit_transition_checks,field:key,model:'SOURCE_AUDITED_INDEPENDENT_POWER_SET_CACHE_V3',candidate_count:candidates.length,audit_cache_key:auditKey,source_audit:audit.source_audit};\n  }\n  function countEwForest(universe,parentByChild){\n    const nodes=new Set(universe);\n    for(const [child,parent] of parentByChild)if(!nodes.has(parent))nodes.delete(child);\n    let changed=true;while(changed){changed=false;for(const [child,parent] of parentByChild)if(nodes.has(child)&&!nodes.has(parent)){nodes.delete(child);changed=true;}}\n    const children=new Map([...nodes].map((id)=>[id,[]]));\n    for(const [child,parent] of parentByChild)if(nodes.has(child)&&nodes.has(parent))children.get(parent).push(child);\n    const visiting=new Set(),done=new Set();\n    const cycle=(node)=>{if(visiting.has(node))return true;if(done.has(node))return false;visiting.add(node);for(const child of children.get(node)??[])if(cycle(child))return true;visiting.delete(node);done.add(node);return false;};\n    for(const node of nodes)if(cycle(node))return null;\n    const selectedWays=(node)=>{let ways=1n;for(const child of children.get(node)??[])ways*=1n+selectedWays(child);return ways;};\n    const roots=[...nodes].filter((node)=>!parentByChild.has(node)||!nodes.has(parentByChild.get(node)));\n    let total=1n;for(const root of roots)total*=1n+selectedWays(root);\n    return {total,nodes:[...nodes].sort(),roots:roots.sort(),parentByChild:new Map([...parentByChild].filter(([child,parent])=>nodes.has(child)&&nodes.has(parent)))};\n  }\n  const ewSubsetValid=(subset,parentByChild)=>{const set=new Set(subset.map(String));for(const [child,parent] of parentByChild)if(set.has(String(child))&&!set.has(String(parent)))return false;return true;};\n  async function exactEwStructural(result,field){\n    if(row.id!=='SER-LIX-EW'||field.key!=='option')return null;\n    const model=await getEwStructuralModel();if(!model)return null;\n    const selection={...(result.selection??{})};delete selection.option;\n    const windowId=selection.window_type,specId=selection.window_spec;\n    const rows=(model.options??[]).filter((one)=>(one.window_id==='*'||one.window_id===windowId)&&(!one['固有仕様ID']||one['固有仕様ID']==='*'||one['固有仕様ID']===specId));\n    const universe=[];const parentByChild=new Map();\n    for(const option of rows){\n      const rel=(model.optionApplicability??[]).find((one)=>one.window_id===windowId&&one.option_id===option.id);\n      if(rel?.applicability==='NON_APPLICABLE')continue;\n      if(!universe.some((id)=>String(id)===String(option.id)))universe.push(option.id);\n      if(rel?.applicability==='CONDITIONAL_APPLICABLE'&&rel.dependency_option_id)parentByChild.set(String(option.id),String(rel.dependency_option_id));\n    }\n    const forest=countEwForest(universe.map(String),parentByChild);if(!forest)return null;\n    const fieldKey=field.key,predictedAllowed=(subset)=>forest.nodes.filter((id)=>!forest.parentByChild.has(id)||subset.map(String).includes(String(forest.parentByChild.get(id))));\n    if(!sameStrings(enabled(field).map((choice)=>choice.value),predictedAllowed([])))return null;\n    const edges=[...forest.parentByChild.entries()].sort(([a],[b])=>String(a).localeCompare(String(b)));\n    const sourceAudit={adapter:'CANONICAL_WORKBOOK_REFERENCE_V1',rule_shape:'single dependency_option_id prerequisite per option',cycle_count:0};\n    const auditKey=hash({model:'EW_OPTION_PREREQUISITE_FOREST',windowId,specId,nodes:forest.nodes,edges,field_required:Boolean(field.required)});\n    let audit=structuralAuditCache.get(auditKey);\n    if(audit){structuralAuditCacheHits++;}\n    else{\n      structuralAuditCacheMisses++;\n      const beforeChecks=terminalMultiTransitionChecks,upstream={...selection};\n      const probes=[[]];for(const id of forest.nodes)probes.push([id]);for(let i=0;i<forest.nodes.length;i++)for(let j=i+1;j<forest.nodes.length;j++)probes.push([forest.nodes[i],forest.nodes[j]]);\n      let verified=true;\n      for(const rawSubset of probes){\n        if(calls>=MAX_CALLS)throw Object.assign(new Error('resolver call limit during EW structural audit'),{code:'RESOLVER_CALL_LIMIT_REACHED',field:fieldKey,calls});\n        const subset=normalizeMulti(rawSubset),next={...upstream};if(subset.length)next[fieldKey]=subset;\n        const child=await resolveRuntimeAppProduct(row.id,next);calls++;terminalMultiTransitionChecks++;\n        const current=(child.fields??[]).find((candidate)=>candidate.key===fieldKey)??null;if(!current){verified=false;break;}\n        const valid=ewSubsetValid(subset,forest.parentByChild);\n        if(valid){if(invalid(child)||!exactSubset(child,fieldKey,subset)||!sameStrings(enabled(current).map((choice)=>choice.value),predictedAllowed(subset))){verified=false;break;}}\n        else if(exactSubset(child,fieldKey,subset)&&!invalid(child)){verified=false;break;}\n      }\n      audit={verified,audit_transition_checks:terminalMultiTransitionChecks-beforeChecks,source_audit:sourceAudit};structuralAuditCache.set(auditKey,audit);\n      console.log('EW_STRUCTURAL_AUDIT window='+row.windowType+' candidates='+forest.nodes.length+' edges='+edges.length+' verified='+verified+' checks='+audit.audit_transition_checks);\n    }\n    if(!audit.verified)return null;\n    const terminal=forest.total-(field.required?1n:0n);\n    return {terminal,custom:customPending(result)?terminal:0n,reachable_state_count:terminal,transition_checks:audit.audit_transition_checks,field:fieldKey,model:'EW_OPTION_PREREQUISITE_FOREST_EXACT_CACHE_V3',candidate_count:forest.nodes.length,edge_count:forest.parentByChild.size,audit_cache_key:auditKey,source_audit:audit.source_audit};\n  }\n  async function exactTerminalMulti(result,field){\n    const cacheKey=terminalContextKey(result,field);\n    if(terminalMultiCache.has(cacheKey)){terminalMultiCacheHits++;return terminalMultiCache.get(cacheKey);}\n    terminalMultiCacheMisses++;\n    const structural=(await exactSamosConflictGraph(result,field))??(await exactSamosStructural(result,field))??(await exactEwStructural(result,field));\n    if(structural){const proof={...structural,context_key:cacheKey};terminalMultiCache.set(cacheKey,proof);return proof;}\n    const statesByKey=new Map(),queue=[];\n    const put=(candidate)=>{const key=terminalStateKey(candidate,field.key);if(statesByKey.has(key))return;statesByKey.set(key,candidate);queue.push(key);};\n    put(result);\n    while(queue.length){\n      if(statesByKey.size>MAX_TERMINAL_BFS_STATES)throw Object.assign(new Error('terminal MULTI_ENUM BFS state limit'),{code:'TERMINAL_MULTI_BFS_LIMIT',field:field.key,state_count:statesByKey.size});\n      const state=statesByKey.get(queue.shift());\n      const current=(state.fields??[]).find((candidate)=>candidate.key===field.key)??null;if(!current)continue;\n      const selected=normalizeMulti(Array.isArray(state.selection?.[field.key])?state.selection[field.key]:[]),selectedStrings=new Set(selected.map(String)),allowed=enabled(current).map((choice)=>choice.value),desired=[];\n      for(const value of allowed)if(!selectedStrings.has(String(value)))desired.push(normalizeMulti([...selected,value]));\n      for(const value of selected)desired.push(normalizeMulti(selected.filter((one)=>String(one)!==String(value))));\n      for(const nextSubset of desired){\n        if(calls>=MAX_CALLS)throw Object.assign(new Error('resolver call limit during terminal MULTI_ENUM proof'),{code:'RESOLVER_CALL_LIMIT_REACHED',field:field.key,calls});\n        const nextSelection={...(state.selection??{})};if(nextSubset.length)nextSelection[field.key]=nextSubset;else delete nextSelection[field.key];\n        const child=await resolveRuntimeAppProduct(row.id,nextSelection);calls++;terminalMultiTransitionChecks++;if(invalid(child))continue;\n        const actual=normalizeMulti(Array.isArray(child.selection?.[field.key])?child.selection[field.key]:[]).map(String);if(stableJson(actual)!==stableJson(nextSubset.map(String)))continue;put(child);\n      }\n    }\n    let terminal=0n,custom=0n;\n    for(const state of statesByKey.values()){const current=(state.fields??[]).find((candidate)=>candidate.key===field.key)??null,subset=normalizeMulti(Array.isArray(state.selection?.[field.key])?state.selection[field.key]:[]);if(current?.required&&subset.length===0)continue;terminal++;if(customPending(state))custom++;}\n    const proof={terminal,custom,reachable_state_count:BigInt(statesByKey.size),transition_checks:terminalMultiTransitionChecks,field:field.key,context_key:cacheKey,model:'UI_REACHABLE_BFS_FALLBACK'};terminalMultiCache.set(cacheKey,proof);return proof;\n  }`,
);

replaceOnce(
  'terminal multi exact handoff',
  `    if(f.dataType==='MULTI_ENUM'&&enabled(f).length>MAX_MULTI){`,
  `    const remaining=userFields(result).filter((candidate)=>candidate.key!=='window_type'&&!done.has(candidate.key));\n    if(f.dataType==='MULTI_ENUM'&&remaining.length===1){\n      try{\n        const proof=await exactTerminalMulti(result,f);\n        const counts={terminal:proof.terminal,custom:proof.custom,symbolic:false};\n        memo.set(key,{counts,signature:sig,selection:stable(result.selection??{}),terminal_multi_proof:{field:proof.field,context_key:proof.context_key,model:proof.model,candidate_count:proof.candidate_count??null,edge_count:proof.edge_count??null,audit_cache_key:proof.audit_cache_key??null,terminal:proof.terminal.toString(),custom:proof.custom.toString(),reachable_state_count:proof.reachable_state_count.toString(),transition_checks:proof.transition_checks,source_audit:proof.source_audit??null}});\n        inProgress.delete(key);return counts;\n      }catch(error){blocker={status:error.code??'TERMINAL_MULTI_PROOF_FAILED',field:f.key,message:error.message,states,calls,detail:{state_count:error.state_count??null}};inProgress.delete(key);return null;}\n    }\n    if(f.dataType==='MULTI_ENUM'&&enabled(f).length>MAX_MULTI){`,
);

replaceOnce(
  'window evidence row',
  `  windows.push({manufacturer:row.manufacturer,series:row.series,product_id:row.id,window_type:row.windowType,status,exact_discrete_terminal_context_count:counts?.terminal?.toString()??null,custom_pending_context_count:counts?.custom?.toString()??null,memo_state_count:states,memo_hit_count:hits,resolver_call_count:calls,max_depth:maxDepth,symbolic_frontiers:frontiers,contract_entries:Object.fromEntries([...depMap.entries()]),blocker});`,
  `  const terminalProofSummary=[...terminalMultiCache.values()].map((proof)=>({field:proof.field,context_key:proof.context_key,model:proof.model,candidate_count:proof.candidate_count??null,edge_count:proof.edge_count??null,audit_cache_key:proof.audit_cache_key??null,terminal:proof.terminal.toString(),custom:proof.custom.toString(),reachable_state_count:proof.reachable_state_count.toString(),transition_checks:proof.transition_checks,source_audit:proof.source_audit??null}));\n  windows.push({manufacturer:row.manufacturer,series:row.series,product_id:row.id,window_type:row.windowType,status,proof_model_version:'RUNTIME_UI_SYMBOLIC_FULL_COVERAGE_V1',count_model_version:'NONTW_RESOLVER_DERIVED_CONTRACT_V2_TERMINAL_STRUCTURAL_CACHE_V4',runtime_manifest_sha256:runtimeEntry?.runtimeManifestSha256??null,exact_discrete_terminal_context_count:counts?.terminal?.toString()??null,custom_pending_context_count:counts?.custom?.toString()??null,memo_state_count:states,memo_hit_count:hits,resolver_call_count:calls,max_depth:maxDepth,symbolic_frontiers:frontiers,contract_entries:Object.fromEntries([...depMap.entries()]),contract_digest:hash(Object.fromEntries([...depMap.entries()])),terminal_multi_proof_context_count:terminalMultiCache.size,terminal_multi_cache_hits:terminalMultiCacheHits,terminal_multi_cache_misses:terminalMultiCacheMisses,terminal_multi_transition_checks:terminalMultiTransitionChecks,structural_audit_cache_size:structuralAuditCache.size,structural_audit_cache_hits:structuralAuditCacheHits,structural_audit_cache_misses:structuralAuditCacheMisses,terminal_multi_proof_digest:hash(terminalProofSummary),terminal_multi_proofs:terminalProofSummary,blocker});`,
);

replaceOnce(
  'proof basis and model',
  `  proof_status:blocked.length?'PILOT_BLOCKED':'PILOT_COMPLETE',\n  proof_basis:'RESOLVER_SOURCE_DERIVED_FIELD_DEPENDENCY_CONTRACT_V2_PLUS_EXACT_RUNTIME_BRANCH_RESOLUTION',\n  pilot_only:true,target_windows:TARGETS,base_window_count:windows.length,`,
  `  proof_status:blocked.length?'BLOCKED':symbolic.length?'BLOCKED_SYMBOLIC_FRONTIER':'COUNTED_CANDIDATE',\n  proof_model_version:'RUNTIME_UI_SYMBOLIC_FULL_COVERAGE_V1',\n  count_model_version:'NONTW_RESOLVER_DERIVED_CONTRACT_V2_TERMINAL_STRUCTURAL_CACHE_V4',\n  proof_basis:'RESOLVER_SOURCE_DERIVED_FIELD_DEPENDENCY_CONTRACT_V2_PLUS_EXACT_RUNTIME_BRANCH_RESOLUTION_PLUS_SOURCE_AUDITED_TERMINAL_MULTI_STRUCTURAL_COUNT_WITH_SIGNATURE_AUDIT_CACHE',\n  pilot_only:false,target_windows:TARGETS,base_window_count:windows.length,`,
);

replaceOnce(
  'report note',
  `  note:'Pilot only. Contracts are proof-side dependency projections derived from the actual registered resolver implementations; Runtime/Product Master data remain read-only. Oversized MULTI_ENUM is handed off as a symbolic frontier and is not treated as counted here.'`,
  `  note:'Targeted non-TW discrete exact recovery V4 with source-audited Samos pairwise option conflict graphs. Product Module terminal MULTI independent power-set counting is enabled only when no selector or metadata-derived selector dependency can observe the terminal field; EW option counting uses the canonical single-prerequisite graph. Empty/singleton/pair Runtime audits are cached by proven structural signature, not sampled. Unsupported shapes fall back to bounded UI-reachable BFS and fail closed. Runtime/Product Master remain read-only. Continuous CUSTOM dimensions are not proven here.'`,
);

await writeFile(OUT, source, 'utf8');
const child=spawn(process.execPath,[OUT.pathname],{stdio:'inherit',env:process.env});
const exitCode=await new Promise((resolve,reject)=>{child.once('error',reject);child.once('exit',(code,signal)=>signal?reject(new Error(`Non-TW exact proof killed by ${signal}`)):resolve(code??1));});
process.exitCode=exitCode;