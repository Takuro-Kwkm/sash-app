name: APW430 Gemini One-Secret LIVE (Legacy Disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy_disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy APW430 one-secret worker disabled
        run: |
          echo 'LEGACY_APW430_ONE_SECRET_LIVE=DISABLED'
          echo 'New LIVE jobs must use Worker Contract v1.1.'
          echo 'Gemini AI Pro: .github/workflows/product-master-antigravity-profile-live.yml'
          echo 'Gemini API: .github/workflows/product-master-gemini-profile-live.yml'
          echo 'Historical APW430 artifacts are retained for audit compatibility only.'
          exit 3
